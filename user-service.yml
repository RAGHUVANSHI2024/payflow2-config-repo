server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://payflow-mysql:3306/payflow_user
    username: root
    password: mysql

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://payflow-eureka:8761/eureka
