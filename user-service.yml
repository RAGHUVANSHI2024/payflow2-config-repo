server:
  port: 8082

  datasource:
    url: jdbc:mysql://localhost:3306/payflow_user
    username: root
    password: mysql

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
