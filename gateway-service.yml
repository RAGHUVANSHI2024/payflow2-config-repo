server:
  port: 8080

spring:
  application:
    name: GATEWAY-SERVICE

  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: auth-service
              uri: lb://AUTH-SERVICE
              predicates:
                - Path=/api/v1/auth/**

            - id: wallet-service
              uri: lb://WALLET-SERVICE
              predicates:
                - Path=/api/v1/wallets/**, /api/v1/transaction/**

            - id: user-service
              uri: lb://USER-SERVICE
              predicates:
                - Path=/api/v1/users/**
eureka:
  client:
    service-url:
      defaultZone: http://payflow-eureka:8761/eureka
